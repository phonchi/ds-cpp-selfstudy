# C++ 先備頁獨立讀者審查（2026-09-06）

本次先由三個全新、互不共享寫作歷史的 GPT-5.6-sol 讀者 context 審查，再由主代理裁定、修正與整合。
讀者設定：學過程式、初學 C++，沒有 Python 或課本前提。審查完整涵蓋 P1–P9 正文、
程式、練習、46 題及 78 張詞彙卡，並逐項檢查互動狀態與各頁之間的先備依賴。

A 完整讀 P1–P3；B 先讀 P1–P3，再深讀 P4–P6；C 先讀 P1–P6，再深讀 P7–P9。
讀者獨立推算題目與輸出，不以既有 HANDOFF／測試通過聲明代替閱讀。
修正後 C 另複核 P5、P7、P8 的關鍵銜接；主代理負責真實瀏覽器驗證。

## 發現與處理

前三組讀者提出前八項；主代理整合補查第九、十項。以下皆已修正，未留待發布後處理。

| 頁面 | 問題 | 處理結果 |
|---|---|---|
| P1 | 編譯指令輸出 main.exe，解說卻寫 main／第二行 | 名稱與各平台執行指令一致，補上存檔與獨立執行方式。 |
| P2 | if 突然省略大括號 | 正文與題目保留大括號，先解釋單一敘述可以省略的規則。 |
| P3 | 別名題先使用未教的 += | 題目改成展開賦值；P1 先介紹 +=，供後頁使用。 |
| P4 | 指標動畫首幀的未建立狀態與旁白矛盾 | 標明尚未執行宣告，並展示動畫自己的獨立片段。 |
| P5 | getline 與 >> 混用時可能先讀到空行，失敗仍印留言 | 補輸入成功檢查及 ws 的作用與行首空白取捨。 |
| P5 | substr 的字元數說法與 UTF-8 邊界說明矛盾 | 正文、詞彙卡統一成 char 單位，不等於可見中文字數。 |
| P7 | 尚未完成緩衝寫入就宣告成功 | close 後才檢查串流狀態；範例檔建立與開啟也檢查失敗。 |
| P7 | throw 題目將本例的 catch 路徑說成通則 | 題幹限縮本例，正文說明尋找 handler、傳遞與未處理時終止。 |
| P8 | 互動沿用的上一節 Counter 缺少 increment | 保留該方法，標明片段來源；拼接編譯得到 a=1、b=12。 |
| P1–P9 | 重寫時漏掉既有選項洗牌，若干頁可固定猜同一位置 | 恢復固定種子的 quiz-shuffle，逐一驗證所有選項與回饋。 |

## 確認無誤

- P1→P3：基礎型別、條件、迴圈、函式結構、作用域、傳值、參考與 const 參考順序成立。
- P4→P6：陣列與指標區別、生命週期、配置／釋放、容器與文字、map 缺鍵新增與 set 去重正確。
- P7→P9：檔案與例外、類別建構、this／物件指標、獨立物件狀態、virtual 與模板的先備關係成立。
- 完整範例使用標準 C++17，不依賴課程標頭；正文、題目與詞卡沒有課本案例或課程使用次數依賴。

## 驗證證據

- `python3 tools/check_prereq.py`：51 個完整程式的編譯／輸出、3 個預期編譯錯誤、
  27 段 JS、資料母檔一致性及跨頁錨點通過。19 個片段不算成完整可執行例。
- `python3 tools/check_prereq_browser.py`：九頁在 Chromium 通過播放、暫停、單步、重設、
  完成與重播；P3 兩種傳參數模式；46 題每一個選項的判定與回饋；詞卡控制；1440px／390px 版面。
- 7 個額外情境通過：P5 EOF；P7 正常寫檔、開檔失敗、延遲寫入失敗，以及正數、負數、非整數輸入。
  延遲失敗是在獨立子程序將 `RLIMIT_FSIZE` 設為 0 並忽略 `SIGXFSZ`，實際讓緩衝資料在關檔時寫入失敗；
  結果為 exit 1、stderr「寫入失敗」、stdout 空字串，沒有提前宣告成功。
- P8 取當前類別與互動片段拼接、重新編譯，確認輸出 `1 12`。
- 全站連結、對比、`git diff --check` 通過；生成器與選項洗牌重跑保持冪等。

上述結論限定本次來源快照與受測環境；不將編譯成功等同完整語義正確，亦不宣稱涵蓋所有 C++ 實作或輔助科技。
發布來源為 GitHub Pages 的 `main` 分支根目錄；本次提交限定先備頁改寫與此審查，既有正課外部資源改動保留在本地。
