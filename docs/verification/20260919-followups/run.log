# 第二章改版的五項 follow-up — 驗證紀錄

日期：2026-09-19
環境：Ubuntu 24.04 on WSL2、g++ 13.3.0、Python 3.13、Playwright chromium

起因：`20260919-ch2-overhaul` 交付時列了五項已知未處理的問題，使用者要求一併修掉。
其中第一項在動手後發現規模比原先評估的大 **10 倍**，詳見下方第 1 節。

---

## 1. 程式碼區塊縮排塌陷 —— 不只第一章，是全站

### 原本以為的範圍

follow-up 寫的是「`introduction.html` 有 74 行程式碼落在 `.line` 之外」。

### 實際範圍

`tools/enrich/enrich_lib.py` 的 `hl()` 把 Pygments token 先包 `<span>` 再依 `\n` 切行。
註解與 `#include` 行的 token 自帶換行，所以 span 會跨行，切行之後該行的內容就掉到 `.line` 之外。
`white-space:pre` 只掛在 `.line` 上（`.pseudo-code` 本身沒有 `white-space`），
於是那些行的縮排塌掉、還多出空行、`data-l` 編號跳號或重號。

全站盤點結果：

| | 區塊 | 行 |
|---|---|---|
| `<div class="pseudo-code">` | 180 | 762 |
| `<pre class="pseudo-code">`（可執行範例） | 5 | 46 |
| **合計** | **185** | **808** |

分布在 **18 個頁面**：`00a`、`00b`、`arrays`、`graphs`、`introduction`、`linear_structures`、
`linked_lists`、`p1`–`p9`、`recursion`、`searching_sorting`、`trees`。
（`analysis.html` 在上一次交付時已修好。）

### 修法

1. 修 `hl()`：token 內含 `\n` 時先切段再各自包 span，確保每個 span 都在同一行內開闔。
2. 順手讓 `hl()` 去掉 `lex()` 在結尾多補的空行（區塊中間的空行仍保留）。
3. 對每個**結構壞掉**的區塊：還原成純 C++（去標籤 + `html.unescape`）→ 用修好的 `hl()` 重新產生。
   結構正確的區塊一律不動。

### 安全性驗證

- **逐行動畫零風險**：修復前先掃過全站，確認「結構壞掉」且「被 `hlLine()` 逐行動畫引用」的區塊是 **0 個**。
  腳本另外加了保險：遇到被 `hlLine` 引用的 id 一律跳過。
- **程式碼內容零改變**：每個區塊在寫入前 assert「還原後的純文字 == 原純文字」（忽略行尾空白與尾端空行）。
  修復後再整批比對一次：**有實質差異的頁面 0 個**。
- **移除的 14 個 `<span class="kw"></span>` 是空的**——舊 bug 的殘骸，不是真實上色。
  逐一比對所有上色關鍵字，唯一的差異就是這 14 個空 span。
- **`data-l` 跳號是 bug 造成的**：例如 `p8_oop_basics.html` 有一段 `data-l` 是 `…5, 6, 8, 9, 10, 12…`，
  缺的 7、11 正是被吞掉的註解行。重新產生後還原成連號，比原本更正確。

### 結果

| | 修復前 | 修復後 |
|---|---|---|
| 全站程式碼行 | 5153 | 4976（少的是尾端多餘空行） |
| 結構不正確的行 | **808** | **0** |
| 空的上色 span | 14 | **0** |

瀏覽器實測（Playwright，22 頁）：

```
全站 361 個程式區塊 / 5078 行
  程式碼落在 .line 之外：0 處
  white-space 未生效（縮排會塌）：0 行
```

渲染文字比對（`innerText`）證實修復有實際效果，例如 `p8_oop_basics.html` 第一個區塊：

```
修復前: ''                       ← 多出一個空行，後續內容整體下移
修復後: 'using namespace std;'
```

`p8` 17 個區塊中有 14 個、`p2_flow_control` 29 個中有 25 個、`linear_structures` 22 個中有 6 個
的渲染結果有變化。

---

## 2. 手機版浮動導覽

原本的 follow-up 寫「ch3–9 共 6 頁沒有隱藏規則」。**這個描述是錯的**，實測後修正：

用 grep 找 `@media(max-width:768px)` 會誤判——那個 media query 只是把 `.float-nav`
**重新排版**（移到底部、隱藏標題、縮小），並沒有 `display:none`。
改用 Playwright 在 390px 實際量 `getComputedStyle(.float-nav).display`，結果是
**9 個頁面**仍然顯示：`00a`、`00b`、`00c`、`arrays`、`linear_structures`、
`linked_lists`、`recursion`、`searching_sorting`、`trees`。

處置：
- 9 頁補上 `@media(max-width:768px){.float-nav{display:none;}}`，插在既有的 768px 重排版區塊之後（與 `introduction.html` 同一位置）。
- `p1`–`p9` 的斷點由 700px 改成 768px，與各章頁一致。

實測結果：

```
390px（應隱藏）：22 頁中有 0 頁顯示
760px（應隱藏）：22 頁中有 0 頁顯示
1440px（應顯示）：22 頁中有 21 頁顯示
```

1440px 少的那一頁是 `index.html`——首頁本來就沒有章節導覽元素，正常。

---

## 3. `00a_why_code.html` 的時鐘

全站只有這一頁用 `high_resolution_clock`，其餘（`analysis`、`p2`、`p5`、`p6`）一律 `steady_clock`。
而第二章正好在教「量耗時要用單調時鐘」，所以這不只是風格不一致，是跟後面章節的教學打架。
技術上也更正確：libstdc++ 的 `high_resolution_clock` 就是 `system_clock` 的別名，會被 NTP 往回校。

- 6 處 `high_resolution_clock` → `steady_clock`（`data-l` 總數前後皆 96，未變動）
- 在第一個範例的 `dx-note` 末尾補一句：
  「這兩段計時程式都用 `steady_clock`：它只會往前走，不會因為系統校時而跳動，量『經過了多久』就該用它，第 02 章會再細講。」
- 前後文全檔 grep 過 `時鐘|chrono|計時|clock`、9 處 quiz 回饋、`data/flashcards_zh/00a.json`，都沒有需要連帶調整的字眼
- 兩支程式實際編譯執行，輸出格式與 `.expected-out` 相符（計時數字本來就跟機器有關）

---

## 4. 詞彙卡張數文案

`data/flashcards_zh/ch1.json` 實際 46 張，文案寫 45。已改 4 處：
`introduction.html` 導讀列、TOC、卡片區標題，以及 `index.html` 的章節卡。

---

## 5. `tools/fix_bare_include.py` 的硬寫路徑

原本 `HDR_DIRS = [os.path.expanduser('~/ds_cpp/Slides/pythonds3/cppds')]`，
目錄不存在就 `sys.exit`，換一台機器 clone 下來整條重生鏈就斷在這裡。

改成三種行為：

| 情況 | 行為 |
|---|---|
| `DSCPP_HEADERS` 有設、指到不存在的路徑 | 報錯，exit 1 |
| 沒設環境變數、預設目錄不存在 | stderr 警告、略過、**exit 0** |
| 目錄存在但沒有任何 `.hpp` | 報錯，exit 1（保留原本的保護） |

「預設目錄不存在就略過」的理由：這一步在全站已處理完畢的狀態下本來就是 no-op
（唯一剩下的裸 include 是 `p1_cpp_basics.html` 的 `greeting.hpp`，那是教學用的自寫標頭，
本來就不該加前綴）；真正需要標頭目錄的是上游的 `enrich_lib.run_cpp()`，
把 gate 放在這一步擋不住任何東西，卻會讓整條鏈斷掉。
使用者明確設了環境變數卻指錯，則屬設定錯誤，不能靜默成功。

`tools/enrich/enrich_lib.py` 的 `DSCPP` 常數同樣改成吃 `DSCPP_HEADERS`，未設時值完全相同。

驗證：五種情境實測（預設／環境變數指到有效目錄／指到不存在路徑／模擬別台機器／指到空目錄），
行為皆符合設計；隔離副本新舊對照輸出逐字相同、冪等；本 repo 前後 `md5sum *.html | md5sum` 未變。

`README.md` 與 `HANDOFF.md` 的對應說明已同步。

---

## 檢查器與測試

```
$ python3 tools/check_links_cpp.py            檢查 22 頁    0 個錯誤 / 0 個警告
$ python3 tools/check_contrast.py                            0 個失敗
$ PYTHONPATH=. python3 tools/check_prereq.py                 0 errors
    （Verified: legacy-compiled=103, pages=9, runs=6, scripts=36）
$ PYTHONPATH=. python3 tools/check_prereq_fidelity.py        0 fidelity errors
$ PYTHONPATH=. python3 tests/test_prereq_fidelity.py         Ran 9 tests — OK
$ PYTHONPATH=. python3 tests/test_prereq_format.py           Ran 4 tests — OK
```

`check_prereq.py` 實際編譯了 103 段先備頁的程式碼，是「程式碼還原仍然正確」最強的證據。

重生鏈（`enrich_analysis.py` → `fix_bare_include.py` → `shuffle_quiz.py` → `apply_zh.py`）
跑完再跑一次，`analysis.html` 無差異，冪等。

---

## 6. 「原始碼」連結改指課程標頭

上一次把八章導讀列的教科書連結改成 Runestone 之後，次連結 `💾 原始碼` 留在
`github.com/pearcej/cppds/tree/master/_sources/<Dir>`——那是教科書的 RST 標記原始檔，
對學生沒什麼用。真正該給的「原始碼」是他們每天 `#include` 的那些檔案。

改成 `💾 課程標頭 pythonds3/cppds` → `https://github.com/phonchi/pythonds3/tree/master/cppds`（八章）。

順帶補上兩個缺口：這個 repo 先前只在 `00b_setup.html` 出現過兩次（而且是當 clone 指令的參數），
站上沒有任何可瀏覽的入口。

- `index.html` 新增一張資源卡「💾 課程標頭 pythonds3/cppds」，說明 `dstimer.hpp`／`anagram.hpp`
  等檔案都在這裡，並註明 Jupyter kernel 已內建複本、只有自己用終端機編譯才需要 clone。
- `README.md` 在「教科書」那行後面補一行「課程標頭」。

`index.html` 教科書資源卡與 `README.md` 的 `[原始碼]` 仍指 `pearcej/cppds`——
那兩處講的就是教科書本身，指向教科書 repo 語意正確，沒有動。

驗證：`https://github.com/phonchi/pythonds3/tree/master/cppds` 實際開啟回 200，
標題 `pythonds3/cppds at master · phonchi/pythonds3 · GitHub`，頁面上看得到 `dstimer.hpp`。
`check_links_cpp.py` 0 錯誤 0 警告、`check_contrast.py` 0 失敗。

---

## 7. 全站回歸驗收（29 個動畫容器 × 22 頁）

見 `../20260919-ch2-overhaul/sitewide_regression.log`。**全部通過、零回歸。**

| 項目 | 結果 |
|---|---|
| 逐行高亮動畫 | 掃出 **29 個** `hlLine` 目標容器／13 頁；實際觸發後 124 次帶行號的請求，**122 次命中**（2 次失敗是下述既有缺陷）。關鍵對照：29 個容器在 HEAD 版與修改後各自解析成 `{data-l → 行文字}`，**29/29 完全相同**——重新產生完全沒碰到任何動畫容器 |
| Console | 22 頁載入＋互動：error／warning／pageerror／requestfailed **全 0**。另注入 `console.error('__PROBE__')` 做正控制，8/8 頁收到，排除「監聽器沒接上」的假陰性 |
| 手機導覽 | 390px 與 760px 下 21/21 頁皆 `none`；1440px 皆 `flex`。760px 也是 `none` 證實 p1–p9 的斷點確實改成 768px 了 |
| 程式區塊渲染 | 361 個區塊，落在 `.line` 外的文字節點 **0**、巢狀 `.line` **0**、`.line` 內含換行 **0**、`white-space` computed **361/361 皆 `pre`**。對照 HEAD：舊版有 6 個區塊真的有文字漏在 `.line` 外（arrays 1、p1 2、p4 2、p8 1），現已全部歸位。用瀏覽器 parser 比對 361 個區塊的 textContent：**359 個逐字相同，2 個不同且只差 `high_resolution_clock`→`steady_clock`**（刻意的修改） |
| 可執行範例 | `data-cpp` 共 10 個（run 7、compile-error 1、fragment 2）。7/7 個 run 區塊的 `data-expected` 完好（含 `&#10;` 轉義與多行中文），還原編譯執行後輸出**逐字相符**；compile-error 區塊仍如預期編譯失敗 |
| 00a 計時程式 | 3 個範例全部編譯執行成功，數量級與格式吻合；記憶體洩漏示範輸出四行逐字相同。`high_resolution_clock` 0 次、`steady_clock` 7 次 |

### 順帶修掉一個既有缺陷（不是這次弄壞的）

`linear_structures.html` 的 `#palCode` 只有 `data-l="1"`~`"7"`，但 `palFrames()` 的兩個結論 frame
用 `line:8`（不是迴文）與 `line:9`（是迴文）。症狀：迴文動畫跑到最後一格時不亮任何一行。
已用 HEAD 版逐行比對確認容器與 JS 兩邊都與修改前逐字相同，屬既有缺陷。

根因是第 7 行被擠成 `    } return true; }` 這種一行三件事的寫法——正是這次改版一直在處理的
「看起來像虛擬碼」問題。所以不採最小修法（把 8、9 都改成 7），而是把它展開成正常排版：

```
7     }
8     return true;
9 }
```

並把 `palFrames` 的行號對回正確語意：不是迴文 → 第 **6** 行（`if (first != last) return false;`）、
是迴文 → 第 **8** 行（`return true;`）。

實測：`racecar` 最後一格亮起 `data-l="8"` → `return true;`；
`abcd` 最後一格亮起 `data-l="6"` → `if (first != last) return false;`；pageerror 0。

---

## 未處理（留給下次）

程式碼字級的不一致是**全站性**的，不只第二章。1440px 下實測：

| 頁面 | 程式碼字級 |
|---|---|
| `analysis.html`（本次已統一） | 13.12px × 17 |
| `trees.html` | 11.2px×1、**11.52px×8**、11.68px、11.84px×4、12.48px、12.8px×4 |
| `arrays.html` | **11.52px×5**、12.16px、12.48px、12.8px×5 |
| `introduction.html` | 11.84px、12.16px×2、12.48px×3、12.8px×29 |
| p1–p9 等 | 多數 12.8px，零星 11.5–12.5px |

`trees.html` 與 `arrays.html` 的最小字級（11.2／11.52px）比改版前的第二章還小。
沒有一併處理是因為第二章那次不是單純調字級——連帶要把程式碼搬出窄側欄
（1440px 下側欄只有 286px 寬），否則字放大就會撐破版面。全站照做需要逐頁判斷版面，是另一件事的規模。

