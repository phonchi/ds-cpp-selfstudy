# 原版先備頁最終驗證

本版架構以 `b97fe81` 為基準，完整內容與獨立閱讀修正見 [恢復紀錄](prereq-original-restoration.md)。

## 最終結果

- `check_prereq_fidelity.py`：九頁，0 errors。原版章節順序、82 張比較表及14張 SVG 幾何保留；搬移／替換例外明列 contract。
- `check_prereq.py`：101 個原版完整程式通過編譯、11 個刻意編譯錯誤符合預期，1 段診斷文字分開分類；另有3個具 metadata 的執行例、1個刻意編譯錯誤與2個片段。36 段 JavaScript 語法及所有 HTML／JSON 同步檢查通過。
- `check_prereq_runtime.py`：100 個驗證案例通過，包括99個實際執行及1個預期連結失敗；另略過4個已標明危險的反例、11個由靜態工具檢查的預期編譯錯誤及1段診斷文字。實際執行包含3個預期終止範例，沒有把它們混稱正常結束。
- `unittest discover -s tests -v`：11 個驗證器回歸測試通過。
- 全站22頁連結檢查：0 errors、0 warnings；對比檢查：0 failures。
- Chromium：九頁全部通過原版控制器每個步驟、滑桿邊界、84道四選一的336個選項回饋、175張中英字卡、1440px／390px 樣式與整頁溢出檢查。
- 人工檢視背景圖示與流程圖截圖；手機寬圖以原字級局部捲動，側邊導覽不再遮住內文。

## 檢查邊界

工具不會取代教學閱讀。原文的錯誤語義、課本依賴與替換後不一致，由作者以外的代理逐段指出並修正。
比對原 commit 的工具不使用後來重寫版的 lesson-trace 快照，不會為了通過檢查而要求改畫原圖。

執行工具在獨立暫存目錄建立來源檔與測試資料，不依賴課程標頭；以 C++17 和 UBSan 執行。
平台相依輸出改檢查可確定的事實，固定輸出則正規化空白後比對。
四個 unsafe 反例仍可閱讀其錯誤與原因，但不拿未定義行為的試跑結果當成答案。

瀏覽器測試使用本地 HTML，阻擋外部請求。Chromium 需在本機沙箱外啟動；遠端字型及公式服務
不屬於這次離線互動檢查的通過宣稱。部署後另核對 Pages 建置 commit 與線上檔案內容。
